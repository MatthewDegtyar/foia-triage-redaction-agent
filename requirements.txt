fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pyyaml==6.0.2
pypdf==5.1.0        # PDF text extraction fallback when poppler's pdftotext is absent
anthropic==0.39.0   # only needed for the LLM judgment layer; pipeline runs without it
